# ===========================================
# RIGOR APP - Variáveis de ambiente
# Copie para .env e preencha os valores.
# ===========================================

# --- Backend (Express) ---
PORT=5000

# Supabase (obrigatório para o backend)
# Dashboard: https://supabase.com/dashboard → seu projeto → Settings → API
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua_anon_key_publica

# Opcional: service role key (admin). Não é usado pelo código atual; use só se precisar de operações server-side com permissão total.
# SUPABASE_SERVICE_KEY=

# CORS: origens permitidas em produção (separadas por vírgula). Ex: https://meuapp.com,https://www.meuapp.com
# Em desenvolvimento local, localhost já é permitido automaticamente.
# CORS_ORIGINS=

# --- Cliente (Expo) - usadas no build e no app em runtime ---
# Desenvolvimento local: use localhost:5000 (o app usará http automaticamente).
# Produção: use o domínio do seu backend (ex: rigor-backend.onrender.com).
EXPO_PUBLIC_DOMAIN=localhost:5000

# Opcional: URL completa da API. Se definida, substitui EXPO_PUBLIC_DOMAIN.
# Ex: https://rigor-backend.onrender.com (sem barra no final)
# EXPO_PUBLIC_API_URL=

# --- Replit (opcional - só se rodar no Replit) ---
# REPLIT_DEV_DOMAIN=
# REPLIT_DOMAINS=
# REPLIT_INTERNAL_APP_DOMAIN=

# --- Drizzle (opcional - só para migrations/push em Postgres próprio) ---
# O app usa Supabase para dados; este schema é legado. Use apenas se for usar Drizzle com outro Postgres.
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
